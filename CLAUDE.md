# CLAUDE.md

See [AGENTS.md](./AGENTS.md) for all guidance in this repo.

Key rules (details in AGENTS.md):

- **Open pull requests against `develop`, not `main`.** `main` is the default branch on GitHub, so PRs default to it — set the base to `develop` explicitly.
- Use **`/guidewright-capture`** and **`/guidewright-review`** when adding or updating docs content.
